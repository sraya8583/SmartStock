# ספר הפרויקט - SmartStock - הנחיות עבודה והתקדמות

מסמך זה מרכז את כל ההקשר הדרוש כדי להמשיך לכתוב את ספר הפרויקט (הנדסאי תוכנה, מה"ט) עבור SmartStock - מערכת ניהול מלאי חכם לחנות/מחסן/ישיבה או מוסד לימודים. נועד לאפשר לסוכן/סשן אחר להמשיך בדיוק מהנקודה הזו, באותה שיטת עבודה.

## חוקי עבודה (חובה לפעול לפיהם)

1. **הנוהל הרשמי הוא הקובע.** קובץ "נוהל לפרוייקט גמר ופרקטיקום.pdf" (הועלה מה-Downloads של המשתמש בתחילת השיחה, נוהל משרד העבודה למה"ט, נובמבר 2024) הוא מקור הסמכות היחיד למבנה, למספור הפרקים ולדרישות הפורמט. **סעיף 1.3** בו מגדיר את 15 הפרקים הנדרשים (1.3.1 עד 1.3.15) לפי הסדר. דרישות פורמט: פונט David גודל 12, מרווח 1.5 בין שורות, מספור עמודים, כריכה עם שם מכללה/סמלילים/מגמה/מסלול/נושא/שם סטודנט/שם מנחה/תאריך.

2. **דוגמאות שהמשתמש שולח הן השראה בלבד, לא סמכות.** המשתמש שולח תמונות פסקאות מקבילות מספר פרויקט של סטודנט אחר, כדי להראות סגנון/מבנה/טון. אין לחקות אותן מילה במילה - יש להתאים לעניין האמיתי של SmartStock, ולוודא תמיד עקביות מול הנוהל הרשמי (סעיף 1) ולא מול הדוגמאות.

3. **כל טקסט המיועד להעתקה לספר הפרויקט - חובה לתת בבלוק קוד נקי ומבודד**, בלי לערבב הערות/שאלות בתוכו. הערות/שאלות הבהרה נכתבות מחוץ לבלוק (לפני או אחרי).

4. **לבסס תוכן על עובדות אמיתיות מהקוד/מהריפו, לא להמציא.** כל פעם שיש אפשרות, לבדוק בפועל בקוד (grep/read) לפני שכותבים טענה טכנית (למשל: האם משהו כבר ממומש, אילו חבילות npm קיימות בפועל, איך נראית פונקציה מסוימת). אם המשתמש מבקש לתאר פיצ'ר שעדיין לא קיים כאילו הוא קיים - לוודא תחילה שהוא אכן מתכוון לממש אותו בפועל לפני ההגשה (ראה "משימות ממתינות" למטה), ולסמן את זה לזיכרון/למסמך הזה כמשימה פתוחה.

5. **שיטת העבודה בפועל:** המשתמש שולח תמונה של הפסקה המקבילה מהפרויקט לדוגמה (לפעמים גם מקטע/דיאגרמה) → אני מנסח את הפרק המקביל עבור SmartStock, מבוסס על עובדות אמיתיות → נותן אותו כבלוק טקסט מוכן להעתקה → המשתמש מאשר/מתקן → ממשיכים הלאה.

6. **דיאגרמות:** עבור כל דיאגרמה (Use Case, Sequence Diagram, Data Flow, טופולוגיה) - קודם נותנים תיאור טקסטואלי, ואז JSON מוכן להעתקה לכלי AI ליצירת תמונות (סגנון: מינימלי, שחור-לבן, ללא צללים/גרדיאנטים, טקסט בעברית). אחרי שהמשתמש מדביק את התמונה שנוצרה בחזרה, יש לבדוק אותה בקפידה מול הקוד בפועל (לא רק מול מה שהתבקש) ולאתר אי-דיוקים לפני אישור סופי.

## התקדמות בפרקי הנוהל (סעיף 1.3)

**הושלמו:**
- 1.3.1 תיאור ורקע כללי
- 1.3.2 מטרות המערכת
- 1.3.3 סקירת מצב קיים בשוק, אילו בעיות קיימות
- 1.3.4 מה הפרויקט אמור לחדש או לשפר
- 1.3.5 דרישות מערכת ופונקציונאליות (5.1 + 5.2)
- 1.3.6 בעיות צפויות במהלך הפיתוח ופתרונות (6.1 + 6.4)
- 1.3.7 פתרון טכנולוגי נבחר - **הושלם במלואו** (7.1 טופולוגיה, 7.2 טכנולוגיות, 7.3 שפות פיתוח, 7.4 ארכיטקטורה, 7.5 חלוקה למודולים, 7.6 סביבת שרת, 7.7 GUI, 7.8 API חיצוניים, 7.9 חבילות תוכנה)
- 1.3.8 שימוש במבני נתונים וארגון קבצים (8.1, 8.2, 8.3)
- 1.3.9 תרשימי מערכת מרכזיים - **הושלם** (9.1 Use Case, 9.2 Sequence Diagram, 9.3 Data Flow) - כולל JSON מוכן ליצירת כל שלוש התמונות; ה-Sequence Diagram עבר בדיקת דיוק מול הקוד ותוקן (גבול טרנזקציה).

**נשאר לכתוב (טרם התחלנו):**
- 1.3.10 תיאור המרכיב האלגוריתמי חישובי (איזו בעיה פותר, איך; איסוף מידע וניתוחים סטטיסטיים)
- 1.3.11 תיאור/התייחסות לנושאי אבטחת מידע (**חשוב**: CLAUDE.md מציין שהסיסמאות מאוחסנות כטקסט גלוי ואין middleware הרשאות על נתיבי המוצרים - צריך לבדוק אם bcrypt/jsonwebtoken שכבר מותקנים (ראה package.json) בפועל בשימוש בקוד ה-auth, ולתאם את הפרק למציאות בפועל, לא למה שרשום ב-CLAUDE.md שאולי מיושן)
- 1.3.12 משאבים הנדרשים לפרויקט (שעות, ציוד, תוכנות, ידע חדש, מקורות מידע)
- 1.3.13 תוכנית עבודה ושלבים למימוש
- 1.3.14 תכנון הבדיקות שיבוצעו (בדיקות תהליכיות ברמת משתמש + בדיקות יחידה)
- 1.3.15 בקרת גרסאות (version control - כנראה Git/GitHub, יש להתייחס לזה בפועל)

## משימות ממתינות למימוש בקוד (טרם קיימות, המשתמש התחייב לבנות לפני ההגשה)

1. **אכיפת הרשאות לפי role.** נכון להיום ה-role (admin/משתמש רגיל) הוא **תצוגה בלבד** (רק ב-`UserProfileModal.jsx`), ואין שום אכיפה בפועל לא בשרת ולא בלקוח (נבדק ב-`ProductsPage.jsx` ו-`productController.js`). נדרש:
   - רק **admin** יכול להוסיף/לערוך/למחוק מוצר (Product CRUD).
   - **משתמש רגיל** יכול רק להוציא/להכניס סחורה על מוצרים קיימים (stock movements).
   - יש לממש גם ב-backend (guard על ה-routes/controllers) וגם ב-frontend (הסתרה/נטרול UI).
   - חשוב: פרקים 2, 5.2 ו-6 בספר כבר מתארים את הפרדת ההרשאות כאילו היא פונקציונלית - יש לוודא שהמימוש בפועל תואם למה שכתוב שם (או לעדכן את הפרקים בהתאם אם המימוש בפועל יצא שונה).

2. **שילוב Brevo (שירות דוא"ל חיצוני, REST API - נקרא ישירות עם fetch, ללא צורך בחבילת SDK ייעודית):**
   - שליחת התראה אוטומטית למנהל כאשר כמות מוצר יורדת מתחת לסף המלאי המוגדר לו (lowStockThreshold) - יש כבר TODO קיים בקוד ב-`recordStockMovement` (server/controllers/productController.js) שמציין את זה במפורש.
   - forgot-password flow עם טוקן זמני בעל תוקף מוגבל, נשלח במייל.
   - פרק 7.8 בספר כבר מתאר את זה כאילו קיים (לבקשת המשתמש, כי הוא מתכנן לבנות את זה "ביום-יומיים הקרובים" מ-2026-08-24) - **יש לוודא מימוש בפועל לפני ההגשה**, אחרת יש פער בין הספר להגנה בפועל.

## עובדות אמיתיות שנקבעו על SmartStock (לבסס עליהן תוכן, לא להמציא)

- **מטרת המערכת:** ניהול מלאי חכם לחנות/מחסן/ישיבה/מוסד לימוד - התראת מלאי נמוך פר-מוצר (סף מוגדר בעת יצירת המוצר, לא סף גלובלי) + בקרת היסטוריה (מי הוציא/הכניס מה, מתי, ולאיזה צורך).
- **Stack:** Node.js + Express + Mongoose (server), MongoDB Atlas (ענן - היחיד שהוא "באוויר"; שרת + לקוח רצים מקומית - local, ואין דרישה בנוהל לפרוס לענן, סעיף 1.3.7.6 מפרט "מקומי" כאופציה חוקית), React + Vite + CSS רגיל (client, ללא Tailwind).
- **ארכיטקטורה:** Routes → Controllers → Models בשרת; client מאורגן לפי features (auth, products) עם הפרדה ל-services (קריאות API), hooks (עטיפת state), components (UI).
- **מודלי נתונים:** User (username, email, סיסמה מוצפנת, role), Product (שם, קטגוריה, מחיר, currentStock, lowStockThreshold), StockMovement (יומן immutable: productId, performedBy, type, quantity, resultingBalance).
- **הפונקציה המרכזית של המערכת:** `recordStockMovement` ב-`server/controllers/productController.js` (שורות 70-117) - מתויגת בקוד עצמו כ"הפונקציה המרכזית". מבוצעת בתוך MongoDB session transaction (`session.withTransaction`) שמקיפה: קריאת המוצר, חישוב היתרה החדשה, בדיקת "אין מספיק מלאי" (newBalance < 0 → שגיאה 400), כתיבת רשומת תנועה חדשה ביומן (StockMovement, לפני עדכון המלאי), עדכון currentStock. יש גם endpoint `/reconcile` שמשווה סכום כל התנועות ביומן מול currentStock בפועל, לגילוי סטיות.
- **פתרון בעיית race condition (עדכון כפול):** כבר קיים ופתור - הטרנזקציה מגנה מפני קונפליקט כתיבה בין שתי בקשות מקבילות על אותו מוצר (MongoDB מזהה קונפליקט ומכשיל אחת מהעסקאות).
- **רספונסיביות (CSS למסכים קטנים):** נבדק - **אין** אף `@media` query בקוד הלקוח (client/src) נכון ל-2026-08-24. זה מתואר בפרק 6 כ"פתרון שיושם בהמשך" - טרם בוצע בפועל.
- **חבילות npm בפועל (package.json, נבדק ישירות):**
  - Server: bcrypt, cors, dotenv, express, jsonwebtoken, mongoose (+ nodemon כ-devDependency).
  - Client: react, react-dom, react-router-dom (+ vite, eslint וכו' כ-devDependencies).
- **DB credentials:** ה-`MONGO_URI` ב-`server/.env` הוא מסוג `mongodb+srv://` - מאמת ש-MongoDB Atlas כבר בשימוש בפועל, לא מקומי. קובץ ה-`.env` אינו עוקב אחר git (בטוח, אינו ב-repo).

## הצעת JSON-ים ליצירת דיאגרמות (נוצרו ואושרו במהלך השיחה)

שלוש דיאגרמות הוכנו כ-JSON מוכן להעתקה לכלי AI ליצירת תמונות, בסגנון אחיד (מינימלי, שחור-לבן, טקסט בעברית RTL):
1. **טופולוגיית הפתרון (7.1):** Client (React, local) → Server (Node/Express, local) → MongoDB Atlas (cloud), חיצים דו-כיווניים.
2. **Use Case (9.1):** שני actors - מנהל (הוספה/עריכה/מחיקה של מוצר, צפייה בהיסטוריה) ומשתמש (הוצאת/הכנסת סחורה, צפייה במוצרים/היסטוריה).
3. **Sequence Diagram (9.2):** רישום תנועת מלאי - כולל תיבת טרנזקציה מלאה (מתחילה מיד אחרי הבקשה הראשונית) ו-alt fragment למקרה של מלאי לא מספיק. **גרסה מאושרת ומדויקת מול הקוד.**
4. **Data Flow (9.3):** כניסה למערכת → בחירת מוצר → הזנת כמות וסוג תנועה → רישום תנועת המלאי (בטרנזקציה) → עדכון מלאי ושמירה ביומן.

(ה-JSON-ים המלאים נמצאים בהיסטוריית השיחה - אם צריך, אפשר לשחזר אותם לפי התיאורים לעיל.)

---
*מסמך זה נוצר אוטומטית ב-2026-08-24 על ידי Claude, לסנכרון בין שיחות/מחשבים.*
